{% extends '::base.html.twig' %}

{% block title -%}{% trans %}Word{% endtrans %}{% endblock %}

{% block body -%}
    <h1>{% trans %}Word{% endtrans %} {% trans %}create.new{% endtrans %}</h1>
    <div style="color: black; background: darkcyan;">
        {{ form(form) }}
    </div>
    <ul class="record_actions">
        <li>
            <a href="{{ path('word') }}">
                <span class="glyphicon glyphicon-list" aria-hidden="true"></span> {% trans %}back.to.list{% endtrans %}
            </a>
        </li>
    </ul>
{% endblock %}
