{% extends '::base.html.twig' %}

{% block title -%}{% trans %}pettype.pettype{% endtrans %}{% endblock %}

{% block body -%}
    <h1>{% trans %}pettype.pettype{% endtrans %} {% trans %}edit{% endtrans %}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pettype') }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span> {% trans %}back.to.list{% endtrans %}
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
